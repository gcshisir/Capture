name: Welcome New Contributor

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

jobs:
  welcome:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: "Thanks for making your first issue and welcome to the community"
          pr-message: "Congrats for making your first pull request and thanks for taking the time to improve the product"
